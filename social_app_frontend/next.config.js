/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'images-sites.web.app'
    ]
  },
  reactStrictMode: true,
}

module.exports = nextConfig

